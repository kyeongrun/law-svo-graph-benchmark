# 데이터

`raw/`에는 비교에 사용할 법령 원문의 **버전 고정 스냅샷**을 둡니다. 현재는 빈 디렉터리이며, 기존 프로젝트의 `data/laws/raw/`와 `manifest.json`을 어떤 기준으로 가져올지는 다음 설계 단계에서 확정합니다.

`source_units/`에는 모든 추출기가 공통으로 사용하는 조문·항·문장 단위 분할 결과를 생성합니다. 이 산출물은 재생성 가능하므로 Git에 포함하지 않습니다.
